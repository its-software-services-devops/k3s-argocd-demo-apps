apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: k3s-workstation
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'k3s-workstation-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: git@github.com:nap-devops/nap-devops-argocd-apps.git
        targetRevision: "{{name}}" #Based on environment
        path: 'manifests/k3s-workstation'
      destination:
        name: in-cluster
        namespace: k3s-workstation
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
