ssh:
  image:
    name: lscr.io/linuxserver/openssh-server
    version: latest # Will change to immutable label 

proxy:
  image:
    name: barneybuffet/tor
    version: latest # Will change to immutable label 

controller:
  image:
    name: busybox
    version: latest # Will change to immutable label 
