promtail:
  enabled: true
  fullnameOverride: promtail-ext
  extraArgs:
    - "-config.expand-env=true"
    - "-client.external-labels=device=$(echo ${DEVICE_ID})" #This not work
  config:
    clients:
      - url: https://475784:${GRAFANA_API_KEY}@logs-prod-011.grafana.net/api/prom/push
  extraEnv:
    - name: DEVICE_ID
      valueFrom:
        secretKeyRef:
          name: promtail-credentials
          key: DEVICE_ID
    - name: GRAFANA_API_KEY
      valueFrom:
        secretKeyRef:
          name: promtail-credentials
          key: GRAFANA_API_KEY
